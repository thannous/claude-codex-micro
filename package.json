{
  "name": "codex-micro-layers",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "configure": "node scripts/configure.mjs",
    "build:profile": "node scripts/build-input-profile.mjs",
    "validate": "node scripts/validate-profile.mjs && node scripts/validate-presets.mjs && node scripts/check-doc-links.mjs",
    "test": "node --test",
    "build:gui": "npm --prefix prototype run build",
    "test:gui": "npm --prefix prototype run test:sites",
    "check": "npm run validate && npm test && npm run build:gui && npm run test:gui"
  },
  "devDependencies": {
    "ajv": "8.17.1"
  }
}
