{
  "name": "codex-micro-layers",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "configure": "node scripts/configure.mjs",
    "build:profile": "node scripts/build-input-profile.mjs",
    "thread-status": "node scripts/thread-status.mjs",
    "lighting": "node scripts/lighting.mjs",
    "probe:lighting": "node scripts/lighting-probe.mjs",
    "agent-keys": "node scripts/enable-agent-keys.mjs",
    "validate": "node scripts/validate-profile.mjs && node scripts/validate-presets.mjs && node scripts/check-doc-links.mjs",
    "test": "node --test tests/*.test.mjs",
    "test:coverage": "node --experimental-test-coverage --test-coverage-lines=70 --test-coverage-branches=70 --test-coverage-functions=70 --test tests/*.test.mjs && npm run build:gui && npm --prefix prototype run test:coverage",
    "prepare:gui": "node scripts/prepare-gui.mjs",
    "build:gui": "npm run prepare:gui && npm --prefix prototype run build",
    "test:gui": "npm --prefix prototype test",
    "check": "npm run test:coverage"
  },
  "devDependencies": {
    "ajv": "8.17.1"
  },
  "optionalDependencies": {
    "node-hid": "3.4.0"
  }
}
